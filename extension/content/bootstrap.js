// ─── Content-script bootstrap (placeholder) ────────────────────────────────
// Site-specific adapters are injected on demand by the service worker via
// chrome.scripting.executeScript, not declared as content_scripts. This file
// exists so future site-wide instrumentation can be added here without
// editing manifest.json.

(() => {
  // intentionally empty
})();
